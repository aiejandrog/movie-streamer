# MovieVault

Personal movie streaming app — FastAPI + React + SQLite/PostgreSQL.

- HTTP Range request video streaming (seekable, 206 Partial Content)
- TMDB metadata import (search, poster, genres, runtime)
- Watchlist + continue-watching + watch progress auto-save
- Dark Netflix-style UI
- Railway deploy ready
